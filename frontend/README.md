# School Management System Frontend

This directory contains a Vite + React frontend for the student record component.

## Run locally

1. Open a terminal in `frontend`
2. Run `npm install`
3. Run `npm run dev`

Then open the local URL shown by Vite.
